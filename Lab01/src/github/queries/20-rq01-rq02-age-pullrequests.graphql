# RQ01 idade | RQ02 pull requests aceitas
fragment RepoAgeAndPullRequests on Repository {
  createdAt
  mergedPullRequests: pullRequests(states: MERGED) {
    totalCount
  }
  totalPullRequests: pullRequests {
    totalCount
  }
}
